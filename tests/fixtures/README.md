# Focused release fixture

`three-body.json` is the canonical three-body PartMode runtime fixture.

The fast browser gate opens this real project through the visible product file
boundary, waits for the OpenCascade worker to settle, and requires exactly three
valid bodies, synchronized document/applied revisions, nonzero triangles, and
zero page, console, network, or CAD errors.
