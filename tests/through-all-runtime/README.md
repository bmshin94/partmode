# Target-bounds Through All corpus

This real schema-5 fixture keeps three independent Through All histories:

- an exact-entity base-plane Cut;
- a classic inline-profile Cut with explicit legacy semantic IDs;
- an exact-entity Cut attached to a persistently named face.

The stock begins 20,000 mm from the origin and spans 25,000 mm. The focused
runtime smoke also moves and resizes it, save/reopens every variant, then
compares warm-checkpoint and fresh-worker canonical BREP plus complete F/E/V
name sets. A fixed-depth Through All tool cannot pass this corpus.
